export const SEARCH_PARAM_KEYS = {
  PAGE: "page", // used for pagination, e.g. page=1
  PER_PAGE: "per_page", // used for pagination, e.g. per_page=10
  SEARCH: "search", // used for search, e.g. search=Smoker
  ID: "id", // used for card id, e.g. id=ST06-008
  COLOR: "color", // used for card color, e.g. color=red
  SET: "set", // used for card set, e.g. set=ST01
  TYPE: "type", // used for card type, e.g. type=character
  COST: "cost", // used for card cost, e.g. cost=3
  CLASS: "class", // used for card class, e.g. class=strawhat
  COUNTER: "counter", // used for card counter, e.g. counter=1000
  POWER: "power", // used for card power, e.g. power=5000
  RARITY: "rarity", // used for card rarity, e.g. rarity=common
}
